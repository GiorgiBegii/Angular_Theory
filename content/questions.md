how u using symbol()

in case objects we saving in stack address of heap.

with call we adding in function user context?

```
	const name = "isi"
	
	function sayHello() {
	  console.log(name);
	}

```

